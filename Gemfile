# frozen_string_literal: true

source "https://rubygems.org"

gemspec

gem "minitest", "~> 5.0"
gem "rake", "~> 13.0"
gem "rubocop", "~> 1.80", require: false
# RuboCop depends on parallel (>= 1.10); parallel 2.x requires Ruby >= 3.3, but
# this gem supports 3.2, so keep the last 1.x line until the floor is raised.
gem "parallel", "~> 1.28", require: false
